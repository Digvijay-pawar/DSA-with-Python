ls = [1, 2, 3, 4, 5]

#list pre-defined methods
# ls.append(6) # add item at last position
# ls.remove(1) # remove item
# ls.index(2) # return index of given item
# ls.clear()  # clear all item from list
# ls.count(1) # count occurence of given item
# ls.insert(1,3) # insert at given index if index is out of bound then add at last position
# ls.pop() # remove last value or given index value
# ls.min() # return smallest value
# ls.max() # return largest value
print(ls)
